export const MainArea = ({ count }) => {
  return <main> MainArea {count}</main>;
};

// es können auch mehrere Promps definiert werden, die Reihenfole spielt keine Rolle
// {count, count2} === {count: count, count2: count2}
